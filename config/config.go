package config

// Config representa as configurações da aplicação.
type Config struct {
	// Adicione campos como MinIO, fila, etc.
}

// LoadConfig carrega as configurações (exemplo, pode ser de env ou arquivo).
func LoadConfig() (*Config, error) {
	// Implementação futura
	return &Config{}, nil
}
